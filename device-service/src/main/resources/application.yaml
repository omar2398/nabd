spring:
  application:
    name: device-service

  datasource:
    url: jdbc:postgresql://localhost:5433/nabd
    username: postgres
    password: omar
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true


server:
  port: 8090