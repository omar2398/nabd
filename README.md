# Getting Started

## How to start
First, you need to run the docker containers, by run the following command in theroot directory of *nabd*:
```
docker compose -v up -d
```

To stop the docker containers you can write:
``` 
docker compose down
``` 

You might have delete the volumes if you are encountering issues with the database.
